
---

```markdown
# FilesNFolders

FilesNFolders converts a high-level structure into a concrete filesystem layout. It supports variable substitution, conditional sections, repetition, dry-runs, and atomic writes so large scaffolds are reproducible and safe.